{
  "name": "tennu-cooldown",
  "version": "2.0.0",
  "description": "A plugin for tennu that provides admin access to normal users for select tennu plugins",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/LordWingZero/tennu-cooldown"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.0.5",
    "moment": "^2.10.6"
  }
}
